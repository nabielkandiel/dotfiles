-- Trivial 1-liner plugin setups
require('nvim-autopairs').setup()
require('ibl').setup()
require('todo-comments').setup({ signs = false })
require('scope').setup()

-- Zero-config plugins (vim-tmux-navigator) do not need setup calls here.
